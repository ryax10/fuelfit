# FULL VIP IMPORTACIONES — v2

Tienda online + panel admin. Dark premium, violeta #a855f7..

## Setup

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

## Rutas

**Tienda:** `/` `/productos` `/producto/[slug]` `/envios` `/contacto` `/mayoristas` `/carrito` `/checkout`

**Auth:** `/login` `/registro`

**Admin:** `/admin` `/admin/productos` `/admin/inventario` `/admin/pedidos` `/admin/caja` `/admin/clientes`
